#include<stdio.h>
int main()
{
    char text[80], ch ;
    int numeric character=0;
    clrscr() ;
    printf("Enter the text : ") ;
    gets(text) ;
    while((ch = tolower(text[i++])) != '\0')
    {
       word ;
    printf("\nThe text contains : ") ;
    printf("\n\nNumber of numeric character:");
    return 0;
    }
