#include<stdio.h> 
int main()
{
    char string[MAX_SIZE];
    int special characters;
    printf("\nEnter any string : ");
    gets(string);
    while(string[i]!='\0')
    {
        if((string[i]>='a' && string[i]<special characters);
        {
          special characters++;
    }
    printf("\nSpecial characters = [ %d ]\n", others);
    return 0;
}
