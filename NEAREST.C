#include <stdio.h>
int main() 
{
int s,digit,sum=0;
scanf("%d",&s);
             if(s%2==0)
             {
             	printf("%d",s);
             }
             else
             {
             	printf("%d",s-1);
             }
	
return 0;
}
